import LoginClient from '@/components/Shared/LoginClient/LoginClient';
import React from 'react';

const Login = () => {
    return (
        <div>
            <LoginClient />
        </div>
    );
};

export default Login;